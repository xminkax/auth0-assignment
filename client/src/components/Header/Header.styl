header {
  background-color: #22252e;
  border-bottom: 1px solid hsla(0, 0%, 100%, .1);
}

.wrapper {
  width: auto;
  max-width: 1016px;
  margin-right: auto;
  margin-left: auto;
  padding-left: 15px;
  padding-right: 15px;
  display: flex;
  align-items: center;
}

.menu {
  margin-top: 41.5px;
  margin-bottom: 41.5px;
  display: flex;
  flex-direction: row;
  width: calc(100% - 266px);
  list-style: none;
  align-items: center;
}

.menu li {
  margin: 0 15px;
  cursor: pointer;
  display: flex;
}

.menu span {
  color: #bfbfbf;
}

.menu .arrow {
  position: relative;
  top: 8px;
  height: 4px;
  width: 8px;
}

.menu .toggle {
  display: inline-block;
  font-size: 10px;
  margin-left: 6px;
  position: relative;
  bottom: 2px;
}

.icon-budicon-460 {
  font-size 9px;
  opacity 0.7;
  color: white;
  margin-left: 4px;
}

.logo {
  width: 100px;
  height: 36px;
}

.menu .talk-button {
  text-transform: none;
  font-size: 14px;
  color: #bfbfbf !important;
  box-shadow: inset 0 0 0 1px hsla(0, 0%, 100%, .2) !important;
}

.auth-buttons .signin-button, .logout {
  color: #bfbfbf;
  padding: 1em;
  margin-right: 3px;
  font-size: 14px;
  font-weight: 400;
  border: 0;
  background-color: transparent;
  outline: none;
}

.auth-buttons .signup {
  font-size: 11px;
  padding: 8px 18px;
}

.auth-buttons .signin-button {
  color: #bfbfbf;
}
