.links {
  display: flex;
  justify-content: center;
}

button.platforms {
  border: 0;
  background-color: transparent;
  outline: none;
}

.separator {
  color: black;
  font-size: 28px;
  font-weight: $font-weight-normal;
}
