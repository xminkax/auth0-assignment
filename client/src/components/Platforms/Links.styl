.links {
  display: flex;
  justify-content: center;
  //background: #f1f1f1;
}

button.platforms {
  border: 0;
  background-color: transparent;
  outline: none;
}

.separator {
  color: black;
  font-size: 28px;
  font-weight: 400;
}
